// The Swift Programming Language
// https://docs.swift.org/swift-book

@main
struct SwiftPlayground {
    static func main() {

        let amount = 18.5 // The original amount paid
        let tip = amount / 5 // The size of the tip in relation to amount

        print ("The amount paid was \(amount), and the tip was \(tip)")
        
        }

    }

